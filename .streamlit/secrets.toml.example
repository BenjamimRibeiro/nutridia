# EXEMPLO de segredos para a versão online (Streamlit Cloud).
# NÃO guardes segredos reais aqui neste ficheiro de exemplo.
# No Streamlit Cloud: App → Settings → Secrets, e cola lá o conteúdo (sem o ".example").
#
# database_url: ligação ao Postgres gratuito (Supabase/Neon). Liga o login
#   automaticamente e torna os dados PERMANENTES.
database_url = "postgresql://utilizador:palavra-passe@host:5432/nomedabasededados"

# Alternativa (sem Postgres): forçar o login na mesma, mas os dados são
# apagados quando o site reinicia. Útil só para experimentar.
# require_login = "true"
